import { Order } from '@/types/order';

// This file is kept as a placeholder for future PDF generation functionality
// Currently, we're only showing a toast message when the user clicks "Download Invoice"
export const generateInvoicePDF = (order: Order): void => {
  // Function kept as a placeholder
  console.log('PDF generation functionality has been removed');
};
